name: gogoship
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts:
  firebase_auth:
  firebase_core:
  cloud_firestore:
  firebase_storage:
  intl:
  cupertino_icons: ^1.0.2
  file_picker: ^6.0.0
  image_picker:
  url_launcher:
  flutter_datetime_picker:
  flutter_map:
  latlong2:
  http: ^1.1.0
  dio: ^5.3.3
  location: ^5.0.3
  geolocator:
  geocoding:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  flutter_launcher_icons:

flutter_launcher_icons:
  android: "launcher_icon"
  ios: false
  image_path: "assets/images/gogoDriver.png"
  min_sdk_android: 21

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
